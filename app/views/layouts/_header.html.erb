<header class="navbar navbar-default navbar-custom navbar-fixed-top">
    <div class="container">
        <%= link_to("e-Learn", root_path, id: "logo") %>
        <nav>
            <ul class="nav navbar-nav pull-right">
                <li><%= link_to("Home", root_path) %></li>
                <li><%= link_to("Help", help_path) %></li>
                <% if logged_in? %>
                <li><%= link_to "Tous les utilisateurs", users_path%></li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    Log in <b class="caret"></b> 
                    </a>
                   
                    <ul class="dropdown-menu">
                        <li><%= link_to "Profile", current_user %></li>
                        <li><%= link_to "Paramètre du compte", edit_user_path(current_user) %></li>
                        <li class="divider"></li>
                        <li><%= link_to "Log out", logout_path, method: "delete" %></li>
                    </ul>
                </li>
                <% else %>
                <li><%= link_to "Log in", login_path %></li>
                <% end %>
            </ul>
        </nav>
    </div>
</header>